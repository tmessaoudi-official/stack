#!/bin/bash
set -euo pipefail

sudo mkdir -p "${GLOBAL_STACK_DOCKER_ROOT_PATH}" "${GLOBAL_STACK_DOCKER_WORKDIR}" "${GLOBAL_STACK_DOCKER_TOOLS_PATH_BIN}" "${GLOBAL_STACK_DOCKER_TOOLS_PATH_LOCKS}" "${GLOBAL_STACK_DOCKER_TOOLS_PATH_VERSIONS}" "${GLOBAL_STACK_DOCKER_TOOLS_PATH_SUCCESSES}" "${GLOBAL_STACK_DOCKER_TOOLS_PATH_ERRORS}" "${GLOBAL_STACK_DOCKER_TOOLS_PATH_SHELLRC}" "${GLOBAL_STACK_DOCKER_TOOLS_PATH_CACHE}" "${CAROOT}" "${GOROOT}" "${GOPATH}" "${GLOBAL_STACK_ZIGPATH}" "${GLOBAL_STACK_SSL_PATH}"
