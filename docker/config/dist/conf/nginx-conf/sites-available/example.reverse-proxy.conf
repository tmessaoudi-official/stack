server {
    # listen 8080;
    # server_name tool.local;

    # # Logging
    # error_log logs/tool.local-http_error.log;
    # access_log logs/tool.local-http_access.log;

    # # Proxy settings for phpMyAdmin
    # location / {
    #     proxy_pass http://tool/;  # Adjust this to the correct upstream if necessary
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }
}