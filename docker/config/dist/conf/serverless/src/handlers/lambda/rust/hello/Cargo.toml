[package]
name = "rust_lambda"
version = "0.1.0"
edition = "2021"

[dependencies]
lambda_runtime = "0.11"
lambda_http = "0.11"
serde_json = "1.0"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
rustc_version_runtime = "0.2"  # For getting Rust version